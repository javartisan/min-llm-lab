# 原始模型 vs 微调模型 对比测试报告

- 生成时间: `2026-09-09 15:47:01`
- 原始模型: `/Users/tengdelong/PycharmProjects/SmolLM2-135M-sft/models/SmolLM2-135M`
- 微调模型: `/Users/tengdelong/PycharmProjects/SmolLM2-135M-sft/models/smollm2-135m-java`
- 测试集: `/Users/tengdelong/PycharmProjects/SmolLM2-135M-sft/data/test.jsonl`（12 条）
- 生成参数: `max_new_tokens=256`, `temperature=0.7`, `top_p=0.9`

## 指标说明

| 指标 | 含义 | 方向 |
|---|---|---|
| NLL | 参考答案在模型下的平均负对数似然 | 越低越好 |
| Token F1 | 生成文本与参考答案的字符集合 F1 | 越高越好 |
| Keyword Hit | 参考答案关键片段在生成中的命中率 | 越高越好 |
| Latency | 单条生成耗时（秒） | 越低越好 |

## 汇总对比

| 指标 | 原始模型 | 微调模型 | 更优方 |
|---|---:|---:|---|
| NLL | 1.8091 | 1.7521 | 微调模型 |
| Token F1 | 0.1575 | 0.1888 | 微调模型 |
| Keyword Hit | 0.0842 | 0.1286 | 微调模型 |
| Latency (s) | 11.093 | 8.912 | - |

## 逐条生成对比

### 用例 1

**Prompt:** 请解释一下 Java 里的 interface 是什么？

**Reference:** interface 用于声明一组方法规范，供类去实现。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | ><br>>   * `interface` 允许继承，可以定义一个接口。这个类被称为实现类（Base Class）和其他子类(Subclass)像是对应的方法而不是属性等级来处理缺陷了。<br>>   <br>>   * `abstract` 是比较特殊的一个接口。简单地说，Abstract Interface 是抽象的，所有成员都必须要在抽象类中使用。<br>>     如果已经提到了Abstract Interface，则会出现`final`逗号后面的内容。例如：<br>>      public abstract void add(int a); // 直接将a放进去<br>>      <br>>      <br>> 你可能会问我是否希 | 1.8300 | 0.2500 | 0.2143 | 11.005 |
| 微调 | //interface 中有一个静态成员，这个是接口(Interface)。接口和类都将对象作为实例化的对象，并且方法可以直接调用实例（即看到实例）展示出来。如果我们不懂 Java API 预处理功能，大家可以参考此文章：[Java 中的 Interface](http://www.jianshu.com/p/1f5c7843b9e6).<br>//通过上面的内容来说，我们已经了解到类、interface 和 interface 的区别。关于 Java 的 interface 是什么？<br>//java.lang.Object 是 Java 版本 2007 引入的响应式标准所提供的 | 1.7439 | 0.2317 | 0.1429 | 12.200 |

### 用例 2

**Prompt:** Java 接口能不能用 new 创建对象？

**Reference:** 不能，interface 不能直接被实例化。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | - 【原则】渲染者是如何知道已经完成了过滤函数，这个动作将会在什么时候执行。<br>- 实际上，我的观点是：新渲染者可以使用 Vue.js 的一个接口来更好地处理新渲染事件，且它的接口应该简单一样的，所有开发人员都应当设置到 this 中定义。<br>  - 在其他渲染方式下，Vue.js 的 API 和 React.js 的 API 面向 JavaScript 流程，实现递归，毫无直接项目性能问题和非常 | 2.2661 | 0.1769 | 0.1250 | 10.411 |
| 微调 | - 使用 new 关键字时，实现一个函数的特殊情况：<br><br>  - 所有父类定义了具体新引用的属性，而子类无法访问。<br>  - 父类中可以放置一些指向子类组件上面所做的动作(或是在当前方法里)。 | 2.1962 | 0.1364 | 0.0000 | 6.029 |

### 用例 3

**Prompt:** 继承在 Java 中有什么作用？

**Reference:** 子类可以通过继承复用父类已有的属性和方法。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | Java 特点：<br>- 实现了类的层次结构，包括语法、数据结构、对象算法等。（如果一个类是可以定义的一种实现方式）<br>- 当编写代码时，使用这些抽象的特性将变得太快了吧！<br>  - 至于利用接口来更好地去设计我们所需要的功能上面还有什么会带来最大影响呢？<br><br>1. 对象源<br>- 对象具有两个绝对不同的核心：(i)独立性和(ii)独立行为<br><br><br>2. 可见性 | 1.5152 | 0.1606 | 0.1053 | 12.727 |
| 微调 | - 优点：确保类型高度的不受限制，可以更快地将业务逻辑放入了类层面。<br>- 缺点：允许重写和实现而不是原型方法<br>```java<br>public interface MyInterface {<br>    void doSomething(); // 此时 doesSomething() 函数应该被覆盖在接口定义中<br><br>	// 注意：如果我们使用 extends 来包含 Interface 节点，这些子interface 都得自己触发 doesSomething() 函数。<br>}<br>``` | 1.3113 | 0.1471 | 0.1053 | 8.565 |

### 用例 4

**Prompt:** 请简单说明 Java 多态的含义。

**Reference:** 多态指同一行为在不同对象上可以有不同实现。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | // 注意：不要使用正则表达式、空格或逗号分割，这是为了提高错觉复杂度的优化和可扩展性；<br>	public static void main(String[] args) {<br>		System.out.println("Hello World!"); // Output: Hello World!<br>	}<br><br>	/**<br>	 * 1. 符号（operator）<br>	 * 2. 连接符 (comma)<br>	 * 3. 双括号内元组（array and array element, string and string elements...etc...)<br>	 * 4. 数组的每个元素（key=value, name=age...)<br>	 * 5. 数组的赋值（增加主成员成员名称，将存放在里面的值对应一个数据类型）<br>	 * 6. 数组的取值 | 1.7479 | 0.0845 | 0.0000 | 11.433 |
| 微调 | Java 中的多态和基类是独立的，它们主要用于在一个对象上访问不同动作时进行相应的细节，这样可以避免采用“定制原型”等技术来分配特定的属性或者方法。另一种解决方案就是使用构造函数、指针或者引用。当你有很多静态的属性，依旧是建模为了保存数据的地方；每个构造函数都会自己的拷贝到子类中。所以构造函数最好能够找到具有更大的功能的组件。<br>### 概 | 1.7380 | 0.2286 | 0.2778 | 10.538 |

### 用例 5

**Prompt:** abstract class 和 interface 有什么区别？

**Reference:** abstract class 可以有实现代码，interface 更侧重定义规范。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | > 2. 所谓实现的接口，是指所有方法都能处理一个类型。而容器应该称为继承框架（或者就算是抽象类），这个定义相当于使用不同的物体进行了解决问题，每个容器可以自己对数据和属性进行更改。<br><br>3. interface和容器具有一套独立的“单元流”(Interface)通信服务，在客户端中使用容器时必须先设置单元流。因此, 面向对象、面向那些排列状态的类似的看上去 | 2.1009 | 0.2405 | 0.2667 | 11.042 |
| 微调 | ## Java API 概述<br>Java API 是英文类型，将程序抽象化成一个接口。它可以让你在没有使用语言的环境下开发应用工具而不再需要专门掌握语言和框架的能力。这种简单度产生了很多人相信为此大众必须学到一些常见的方法和功能来更好地掌握或找出从后期各个模块中提取出最终的结果。当前 java.lang.System 编译时无需采用 JDK1.6 虚拟机特性的新特性（JDK1.7）并非本 | 2.0515 | 0.1503 | 0.0667 | 10.814 |

### 用例 6

**Prompt:** 一个类可以实现多个 interface 吗？

**Reference:** 可以，Java 支持一个类实现多个接口。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | - 什么是interface?<br>- 类继承, 抽象类继承, 接口继承.<br>- 类型三角形，具体怎样做的情况下面都有点同步/不同吧。<br>- 纯粹上层级和分布式级别的区别：<br>  - 在本地编译时未收集到的空间等于0.5%，这就比较无法使用在线测试中了（因为一定要对应的稳定特征）<br>  - 纯粹上层级和分布式级别的区别:<br>    * 如果把所有上层级的数据均值(或者方差)设置成0. | 1.5566 | 0.1151 | 0.0833 | 12.188 |
| 微调 | ### 4. 浏览器中使用什么接口和方法<br><br>```java<br>  public class A implements B {<br>    private B b;<br><br>    public A(B obj){<br>      this.b = obj;<br>    }<br>    <br>    @Override<br>    public void foo() {<br>      System.out.println("A: " + b);<br>    }<br>  }<br><br>  public class C extends A implements B {<br>    private B b;<br><br>    public C(B obj){<br>      this.b = obj;<br>    }<br>    <br>    @Override<br>    public void foo() {<br>      System.out.println("C: " + b);<br>    }<br>  }<br>  <br>  public class D extends C, A {<br>    private A a;<br>    <br>    public D(B obj){<br>      this.a = obj;<br>    }<br>    <br>    @Override<br>    public void foo() {<br>      System.out.println("D: " + a);<br>    }<br>  }<br>  <br>  //D 触发其他方法，而这种语言就是不同的。在一些情况下，被选 | 1.6277 | 0.1600 | 0.1667 | 12.422 |

### 用例 7

**Prompt:** Java 中 extends 关键字用来做什么？

**Reference:** extends 用于声明类继承另一个类。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | */<br><br><br>class MyClass {<br>    // 打印出属性名称，方法名称和过滤器：<br>    printName(name){<br>        console.log("hello, " + name);<br>    }<br><br>    printMethod(){<br><br>        console.log('hi');<br>        <br>    }<br>    <br>} | 1.8222 | 0.1818 | 0.0000 | 3.442 |
| 微调 | - **继承** - 节点是一个可以通过子节点的指定结构。<br>  - 两个父节点一样的结果为同一个结构，不存在于它的子节点上。<br>  - 此外，子节点只能使用了这种类型的子节点(当前位置)或者其他节点（尾巴）。<br><br>2. 在 Node 中遍历数据库操作时：<br><br> 1. 首先遍历表单数据项中所有的值。<br><br> 2. 直接把每个值取出来，保证不会重复使用相同值。<br><br> 3. 如果要更改数据的时候，则传入对象。<br><br>    <ul><br>      <li><code> | 1.7210 | 0.1654 | 0.1818 | 11.396 |

### 用例 8

**Prompt:** 方法重写 override 和多态有什么关系？

**Reference:** 子类重写父类方法后，运行时会按实际对象类型调用对应实现，体现多态。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | 1. 最简单的实现一个静态函数，同样使用override<br>2. 适配器模式：将一些属性放在不传参数上。但是这种模式越来越严重。可以让耦合度大大降低<br><br>3. 测试应用场景中:<br>  - 过多的组件/类<br>  - 不必要的外观<br><br>  **注意**<br>   1. 绑定原生接口时, 可以把所有的数据转换成了外观对象<br>      (this)的属性和方法，因此每个对象都包括自己的data属性（或者getData()）<br><br>   2. 选项值(argument value)和setter | 1.7316 | 0.1887 | 0.1111 | 12.162 |
| 微调 | public void doSomething() {<br>    //这是我的实现：<br>    this.doSomeThing();<br><br>    //如果在不想对原型继承了一个接口进行处理，就要考虑使用新类和面向对象模式。所以我说`override`就可能会产生注入的异常。<br>    <br>    //为什么需要new对象？<br>    new Person(10);<br>    <br><br>    //new 自动覆盖父类构造函数中的构造函数。<br>    <br>} | 1.7128 | 0.1860 | 0.1111 | 8.562 |

### 用例 9

**Prompt:** interface 里的方法默认是什么访问权限？

**Reference:** 接口中的方法默认是 public abstract。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | </p><br><h2 id="86957">获取登录用户信息</h2><br><ul class="alert alert-danger" role="alert"><br>    <li><strong>如果您已允许使用这个请返回正常，否则再见。</strong></li><br>    <li><strong>为了更好地理解实现对每一个用户和管理员一致性不断改变，可以直接强制将所有非管理员人员显示在本地或者私有屏幕上。</strong></li><br>    <li><strong>由于通过导入SQL Server 10.0的表 `user` 来探测天末日子，因此会出现数据库中存在的东西（例如： | 2.1790 | 0.1783 | 0.0000 | 12.805 |
| 微调 | - 由于该接口已经引入了，您必须要将 `Permission.getResource` 函数作为参数传递到下一步实现中。<br><br>## 执行时间<br><br>```java<br>public interface IRequestHandler {<br>    void execute(Object... args);<br><br>    void setData();<br>    void cancel();<br>}<br>``` | 2.0479 | 0.3191 | 0.2000 | 4.726 |

### 用例 10

**Prompt:** 父类引用可以指向子类对象吗？

**Reference:** 可以，这是多态中向上转型的常见写法。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | 1.  现在是一个非常易情的做法：<br>```java<br>class Base {<br>    // 定义了一个属性，他会有个值存放到变量中。<br>    private String name;<br><br>    public void setName(String newName) throws Exception{<br>        System.out.println("Set Name: " + name);<br>        System.out.println("New Name: " + newName);<br>        System.out.println("Get New Name: " + name);<br><br>        if (newName == null \|\| name != oldname) {<br>            throw new IllegalStateException("Can't change a non-null value.");<br>        } else {<br>            System.out.println("The new name is: " + name);<br>        }<br>    }<br>    <br>    public void getName(){<br>        System.out.println("getName");<br>    }<br>}<br>Base b = new Base();<br>b.setName("John");<br>System.out.println("Old Name: " + name);<br>System.out.println("New Name: " + b.getName()); | 2.2604 | 0.1400 | 0.0000 | 10.908 |
| 微调 | - 请问，如果一个变量在子类中有任何属性（元素）的值，那么这个变量将不会出现意外访问。 | 2.2550 | 0.1786 | 0.0000 | 2.861 |

### 用例 11

**Prompt:** 为什么说 interface 适合做解耦？

**Reference:** 调用方只依赖接口约定，不依赖具体实现类。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | - 问：我的计算机是没有运行环境变量，实际上需要用到系统间跨越处理器的数据。 当前在 Linux 中使用 gcc 来生成一个相应的代码文件时就会出现这样的问题。<br>  - 文件名称不能匹配，其对应的字符串必须由 gcc 编辑器自动生成，最好写入 “.o” 格式的文件名（“g++ ./a.out”）<br>  - 此命令并非所有权，可以采用 gcc 输出的模块信息改密码和保护了模块信息的控制权，但是 GCC 的标 | 1.5246 | 0.0811 | 0.0000 | 11.278 |
| 微调 | > - 在接口的定义中，可以有一些主要成员方法。这个时候我们就会销毁这些空间了，但是我们还得注意它是不能保存具体的类型的，例如一个接口的所有内容都必须继承自AbstractBase，并且无论这个接口名称出现在interface中都是包装好的，因此当我们重新创建对应的interface后，或者使用new()来创建对象时，Java 导致失败。<br>> - 把Interface和Closure相关的数据分离开来，把枚举的值分离开来，把枚 | 1.4637 | 0.1644 | 0.1333 | 10.924 |

### 用例 12

**Prompt:** Java 单继承限制指的是什么？

**Reference:** 一个类只能直接继承一个父类，但可以实现多个接口。

| 模型 | 生成结果 | NLL | Token F1 | Keyword Hit | Latency (s) |
|---|---|---:|---:|---:|---:|
| 原始 | - 按照这个问题来分析，首先说明一下 C++ 平台的原理。在 C 语言中我们有一个类 A 类型的对象A.class, 而且它包含了某些方法：a()、b().method(), c(c)、d().method()。所以C++ 在每一行的写法上设置了一个非常小的参数: int a = 1;...<br>```cpp<br>  // cout << "Hello world!\n";<br>  <br>  // 我们把一个堆栈的强连接的函数传递到一个堆栈里面<br><br>  // 对象A（A.class）和B（A.method())的使用顺序得出最后一个层级为2017年3月8日发 | 1.1744 | 0.0927 | 0.1053 | 13.719 |
| 微调 | - 有哪些东西都可以用单独实现类型的继承？<br>- 面向对象和原始类型具体不能够相同，而面向对象和普通类型及类间接层级也无法相同。<br><br>3. 推理总结：<br><br>  - 特征集（attribute）<br>  - 依赖关系（dependency）<br>  - 日志（logistic）<br>  - 应用场景(business logic)<br>  - 解决方案 (solution) | 1.1563 | 0.1982 | 0.1579 | 7.908 |

## 结论

- 在参考答案似然（NLL）上，更优: **微调模型**（原始 1.8091 vs 微调 1.7521）。
- 在字符级重叠（Token F1）上，更优: **微调模型**（原始 0.1575 vs 微调 0.1888）。
- 在关键词命中上，更优: **微调模型**（原始 0.0842 vs 微调 0.1286）。
- 本测试使用独立测试集（含改写题与相关新题），未使用训练数据；样本量仍较小，结论仅供参考。
