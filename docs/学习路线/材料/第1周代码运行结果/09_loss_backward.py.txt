# cmd: python learn/week01/09_loss_backward.py
# exit: 0
# captured: 2026-09-11 10:23:20
# ----------------------------------------
模型目录: /Users/tengdelong/PycharmProjects/SmolLM2-135M-sft/models/SmolLM2-135M-Instruct
文本      : Java interface
input_ids : [[39570, 8334]]

============================================================
【1】Forward + Loss
============================================================
loss = 10.3506
这个数是「平均负对数似然」。越大 = 越不像标签；训练就是把它压下去。
到这里权重还没改。model.train() 也没有触发训练循环。

============================================================
【2】Backward：反传，算出梯度
============================================================
Embedding.grad 是否存在 : True
Embedding.grad 形状     : (49152, 576)
梯度的整体大小(L2)      : 43.422653
梯度告诉优化器：每个参数往哪边改、改多少。
这一步没有产生新的回答文本，权重仍未更新。

============================================================
【3】参数更新（真正改权重的是这一步）
============================================================
某个权重更新前 = 0.16503906
某个权重更新后 = 0.16113977
差值           = -0.00389929
公式直觉：weight ← weight - lr * grad

和小项目正式训练的关系：
  - scripts/sft001.py 里的 trainer.train() 才是启动多步训练循环
  - 循环里每一步仍是：Forward → Loss → Backward → optimizer.step()
  - 差别只是：数据是对话、可训练参数可能是 LoRA、优化器一般是 AdamW

小结论：
  - model.train() 是换挡（训练模式），不是踩油门。
  - 油门是 Forward + Loss + Backward + optimizer.step()。
  - Loss 衡量「预测和标签差多远」。
  - loss.backward() 只写 .grad（阅卷）；optimizer.step() 才改权重（改作业）。
  - 只有其中一步、或顺序反了、或不 zero_grad，训练会不动或乱跳。

